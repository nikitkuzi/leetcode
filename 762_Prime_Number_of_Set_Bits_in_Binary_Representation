class Solution:
    def countPrimeSetBits(self, left: int, right: int) -> int:
        prime = {2,3,5,7,11,13,17,19}
        return sum(1 if i.bit_count() in prime else 0 for i in range(left, right+1))

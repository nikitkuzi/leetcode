class Solution:
    def bitwiseComplement(self, n: int) -> int:
        if n == 0:
            return 1
        if log2(n)%1==0:
            return n-1
        return ~((1<<ceil(log2(n)))+n)+(1<<(ceil(log2(n))+1))

class Solution:
    def numSteps(self, s: str) -> int:
        res = 0
        n = int(s,2)
        while n > 1:
            if n % 2:
                n += 1
            else:
                n >>= 1
            res+=1
        return res

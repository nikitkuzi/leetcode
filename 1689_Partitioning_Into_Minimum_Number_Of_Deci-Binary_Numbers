class Solution:
    def minPartitions(self, n: str) -> int:
        res = '0'
        for c in n:
            res = max(res, c)
            if res == '9':
                return 9
        return int(res)
